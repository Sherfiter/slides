# 第 7 章 · 容器技术简介

## 7.1 什么是容器

**容器**是把应用程序及其依赖打包在一起、可随处运行的标准单元。
它不是「轻量级虚拟机」：

- 容器共享宿主机内核，只隔离进程视图
- 虚拟机各自有独立内核与完整 OS

```
  VM:    App ─ Guest OS ─ Hypervisor ─ 硬件
  容器:  App ─(共享)─ Host OS ─ 硬件
```

---

## 7.2 容器背后的两大内核特性

- **Namespace（命名空间）**：隔离「看到什么」——PID、Mount、Net、UTS、IPC、User
- **cgroup（控制组）**：限制「能用多少」——CPU、内存、I/O、进程数

```bash
# 一条命令体验进程与挂载隔离
unshare --pid --mount --fork bash
```

---

## 7.3 容器的发展与应用场景

- 2013 年 Docker 开源，容器走向大众
- 之后出现 Kubernetes 编排、OCI 运行时标准

**应用场景**：

- 微服务部署
- CI/CD 构建与测试环境
- 应用的快速交付与回滚
